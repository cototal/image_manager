{% extends 'base.html.twig' %}

{% block title %}{{ image.displayName }}{% endblock %}

{% block body %}
    <div class="row mt-4">
        <div class="col-md-6">
            <h1>{{ image.displayName }}</h1>
            <img class="img-fluid" src="{{ vich_uploader_asset(image) }}" alt="{{ image.displayName }}">
        </div>
    </div>
{% endblock %}
